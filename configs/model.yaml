# Model configuration
model:
  name: "Qwen/Qwen2.5-3B-Instruct"
  backup: "meta-llama/Llama-3.2-3B-Instruct"
  device: "cuda"
  dtype: "float16"           # float16 or auto
  quantization: null         # null for fp16, "4bit" for bitsandbytes
  load_in_4bit: false
  bnb_4bit_compute_dtype: "float16"
  bnb_4bit_quant_type: "nf4"
  trust_remote_code: true
  max_memory: null           # e.g. {0: "14GiB"} for explicit VRAM limit

# Layer info (Qwen2.5-3B)
architecture:
  num_layers: 36
  hidden_dim: 2048
